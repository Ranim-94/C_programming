

#pragma once
#include<stdio.h>
#include<time.h> // for time
#include<stdlib.h> // to use RAND_MAX

void display_array(float some_arr[], size_t arr_size);

void generate_random_array(float* array_ptr,size_t arr_size);